{
  "name": "arachnea-front",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"PUBLIC,ADMIN\" --prefix-colors \"blue,red\" \"cd public-app && npm run dev\" \"cd admin-app && npm run dev\"",
    "build": "npm run build:public && npm run build:admin",
    "build:public": "cd public-app && npm run build",
    "build:admin": "cd admin-app && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}